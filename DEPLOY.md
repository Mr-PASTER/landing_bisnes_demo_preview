# 🚀 Инструкция по деплою на GitHub Pages

## Быстрый старт

### 1. Подготовка репозитория

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Подготовка к деплою на GitHub Pages"
```

### 2. Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com) и создайте новый репозиторий
2. **НЕ** инициализируйте репозиторий с README, .gitignore или лицензией

### 3. Настройка basePath (если нужно)

**Важно:** Если ваш репозиторий называется `username.github.io`, пропустите этот шаг.

Если репозиторий имеет другое название (например, `landing_bisnes`), откройте `next.config.ts` и раскомментируйте строки:

```typescript
basePath: '/landing_bisnes',  // замените на название вашего репозитория
assetPrefix: '/landing_bisnes/',
```

### 4. Подключение и пуш на GitHub

```bash
# Добавьте удаленный репозиторий
git remote add origin https://github.com/username/repository-name.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

### 5. Настройка GitHub Pages

1. Перейдите в **Settings** вашего репозитория на GitHub
2. В боковом меню выберите **Pages**
3. В разделе **Source** выберите **GitHub Actions**
4. Сохраните настройки

### 6. Автоматический деплой

После настройки GitHub Pages:
- GitHub Actions автоматически соберет и задеплоит ваш сайт
- Проверьте вкладку **Actions** для отслеживания процесса
- После успешного деплоя сайт будет доступен по адресу:
  - `https://username.github.io/` (для репозитория username.github.io)
  - `https://username.github.io/repository-name/` (для обычного репозитория)

### 7. Обновление сайта

При каждом пуше в ветку `main` сайт будет автоматически обновляться:

```bash
git add .
git commit -m "Обновление сайта"
git push
```

---

## 📝 Что было настроено

- ✅ `next.config.ts` - настроен статический экспорт
- ✅ `.github/workflows/deploy.yml` - GitHub Actions для автоматического деплоя
- ✅ `public/.nojekyll` - файл для корректной работы с GitHub Pages
- ✅ `.gitignore` - исключение ненужных файлов из репозитория

## 🆘 Решение проблем

### Стили или ресурсы не загружаются

Проверьте, что `basePath` и `assetPrefix` правильно настроены в `next.config.ts`

### Ошибка 404

Убедитесь, что:
1. В настройках GitHub Pages выбран источник **GitHub Actions**
2. Workflow успешно выполнен (проверьте вкладку Actions)
3. Путь `basePath` соответствует названию репозитория

### Deployment не запускается

1. Проверьте, что файл `.github/workflows/deploy.yml` присутствует в репозитории
2. Убедитесь, что у репозитория есть права на деплой (Settings → Actions → General → Workflow permissions → Read and write permissions)

---

## 🎉 Готово!

Ваш сайт готов к работе на GitHub Pages!

